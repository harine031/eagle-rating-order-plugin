# 開発ログ

## [2026-08-09] - v1.2.0 公開準備

### 追加・実装 (Added/Implemented)
- manifestのバージョンを`1.2.0`へ更新し、READMEへ今回の更新内容と導入対象ファイル名を反映した。
- GitHub Release添付用パッケージとnote記事更新文を作成する準備を開始した。

### 検証 (Validation)
- `Flexible-Sort-Board-1.2.0.eagleplugin`に必要な5ファイルだけが含まれること、公開用コピーとソースのSHA-256が一致すること、両方のJavaScript構文チェックに合格することを確認した。

## [2026-08-09] - Eagle PNG差し替え結果判定の互換性修正

### 変更・修正 (Changed/Fixed)
- `item.replaceFile()`の戻り値が偽値の場合に即失敗としていた判定を廃止した。
- Eagleのバージョン差に左右されないよう、差し替え後の元PNGを再読込し、対象メタデータチャンクが消えていることを実データで検証する方式へ変更した。

### 検証 (Validation)
- `node --check`によるJavaScript構文チェックと`git diff --check`に問題がないことを確認した。

## [2026-08-09] - リネーム後半名の履歴保存

### 追加・実装 (Added/Implemented)
- 「後半の名前」の入力値をフォルダごとにlocalStorageへ保存し、次回読み込み時に復元するようにした。

### 変更・修正 (Changed/Fixed)
- フォルダを切り替えた場合は、そのフォルダで最後に使用した後半名を表示し、履歴がない場合は空欄にするようにした。

### 検証 (Validation)
- `node --check`によるJavaScript構文チェック、フォルダ別localStorageキー、`git diff --check`に問題がないことを確認した。

## [2026-08-09] - PNG情報削除の任意化とリネーム進捗表示

### 追加・実装 (Added/Implemented)
- PNG情報削除を初期OFFのチェック式にし、リネーム確認画面で実行内容を明示するようにした。
- リネーム対象件数、現在処理中の画像名、完了件数を進捗バーへ表示するようにした。
- PNG差し替えは60秒、名前保存は30秒でタイムアウトし、停止理由を画面へ表示するようにした。

### 変更・修正 (Changed/Fixed)
- PNG差し替え・名前保存で例外が起きた際、表示が「リネーム中…」のまま残る問題を修正した。
- 処理中の二重実行を防止し、エラー時も完了済み件数を保持して一覧を再描画するようにした。

### 検証 (Validation)
- `node --check`によるJavaScript構文チェック、HTMLとJavaScriptの要素ID対応、`git diff --check`に問題がないことを確認した。

## [2026-08-09] - 並び替え方法の保存とPNG情報除去

### 追加・実装 (Added/Implemented)
- 前回選択した並び替え方法をフォルダごとに保存し、次回起動時も同じ方法で復元するようにした。
- PNGの連番リネーム時に、テキスト・EXIFなどのメタデータ用チャンクを除去して元ファイルを差し替える処理を追加した。

### 変更・修正 (Changed/Fixed)
- 保存済みの手動順は手動モードで復元し、名前順などの自動ソートは保存したソート方法を再適用するようにした。

### 検証 (Validation)
- Node.js構文チェックを実施する。

## [2026-08-02] - GitHub公開とv1.1.1 Release作成

### 追加・実装 (Added/Implemented)
- プロジェクトを独立したGitリポジトリとして初期化し、GitHubの公開リポジトリ `harine031/eagle-rating-order-plugin` へ公開した。
- MIT Licenseを追加した。
- GitHub Release `v1.1.1`を作成し、`Flexible-Sort-Board-1.1.1.eagleplugin`を無料ダウンロード用アセットとして添付した。
- READMEへGitHub Releasesからの導入手順、不具合報告先、ライセンス、支援受付準備中の案内を追加した。
- Ko-fiの公開ページ `https://ko-fi.com/harine031` をREADMEとGitHub Sponsorボタンへ追加した。

### 変更・修正 (Changed/Fixed)
- AI用ローカル設定と生成済み公開物の重複を`.gitignore`で除外し、ソース、説明、ライセンス、開発ログだけをGit管理対象にした。
- GitHub Sponsorsは未有効のため掲載せず、先にKo-fiで任意支援を受け付ける構成とした。

### 検証 (Validation)
- ルートと公開用コピーの`index.js`、`manifest.json`、`logo.png`が一致することをSHA-256で確認した。
- ルートと公開用コピーのJavaScript構文チェックに合格した。
- Releaseアセットが公開状態で、GitHub APIからダウンロード数を取得できることを確認した。

## [2026-07-26] - BOOTH公開用ZIPと商品説明の作成

### 🚀 追加・実装
- `publish/BOOTH-Flexible-Sort-Board-1.1.0.zip`を作成し、プラグイン本体と導入READMEをまとめる準備を行った。
- `publish/booth-description.md`を追加し、BOOTHの商品説明文を作成した。
- 公開パッケージのREADMEへ、評価順から手動モードへ切り替えて並べ替えられる特徴を追記した。

### 🧠 開発経緯・思考プロセス
- Eagle標準の評価順ソートと競合する説明を避け、評価順で整理した後に手動で順番を調整できる点を主な価値として訴求した。

## [2026-07-26] - note用サムネイルv3作成

### 🚀 追加・実装
- `publish/note-thumbnail-v3.png`を追加し、「評価順で並べる」から「手動へ切り替えて自由に並べ替える」流れを表現した。

### 🔧 変更・修正
- 評価順だけが主機能に見えないよう、評価順と手動モードの2段階表示、ドラッグ＆ドロップ矢印、「手動で並べ替え」の訴求を追加した。

## [2026-07-26] - note用サムネイル作成

### 🚀 追加・実装
- `publish/note-thumbnail.png`を追加し、プラグイン名、評価順の画像整理、ドラッグ＆ドロップによる手動調整が伝わるnote用サムネイルを作成した。

### 🧠 開発経緯・思考プロセス
- noteの一覧画面でも内容が伝わるよう、左側にタイトル、右側に評価順で整理された画像一覧UIを配置した横長構成にした。

## [2026-07-26] - note公開用記事の作成

### 🚀 追加・実装
- `publish/note-article.md`を追加し、BOOTHでの無料配布案内、主な機能、開発のきっかけ、導入方法、注意事項をまとめた。

### 🔧 変更・修正
- 実際のBOOTH商品URLは未確定のため、記事内に差し替え用リンクを記載した。

### 🧠 開発経緯・思考プロセス
- 画像を評価順に並べ替えたあと、手動で順番を調整したいという開発理由が伝わる構成にした。

## [2026-07-25] - 公開用パッケージ作成

### 🚀 追加・実装
- 利用者向けの`README.md`を追加し、導入方法、操作方法、ショートカット、注意事項を記載した。
- manifestのバージョンを`1.1.0`へ更新した。
- `publish/Flexible-Sort-Board-1.1.0.eagleplugin`を作成した。

### 🔧 変更・修正
- 公開パッケージには`manifest.json`、`index.html`、`index.js`、`logo.png`、`README.md`のみを含め、開発ログや作業用ファイルを除外した。

### 🧠 開発経緯・思考プロセス
- Eagleへそのまま読み込める`.eagleplugin`形式と、展開して確認できる`publish/Flexible Sort Board`フォルダの両方を用意した。
- 圧縮ファイルのエントリ確認とJavaScript構文チェックを実施済み。

## [2026-07-25] - 評価フィルター件数表示

### 🚀 追加・実装
- 評価フィルターの「すべて」「評価なし」「★1〜★5」に、現在の該当画像件数を表示するようにした。

### 🔧 変更・修正
- 評価変更、再読み込み、ゴミ箱移動後にも件数を再計算するようにした。

### 🧠 開発経緯・思考プロセス
- フィルターを選ぶ前に各評価の偏りを把握できるよう、選択肢自体へ件数を表示する方針にした。

## [2026-07-25] - 評価保存経路とプレビューUIの改善

### 🚀 追加・実装
- 評価変更時にEagleローカルAPIの`/api/item/update`へ`star`を送信する処理を追加した。
- 評価更新後に`eagle.item.getById()`で対象を再取得し、画面側の状態もEagle本体と同期するようにした。

### 🔧 変更・修正
- `item.star = value; item.save()`だけではEagle本体への反映が確認できなかったため、評価専用の更新APIを利用する方式へ変更した。
- プレビュー操作アイコンを右下へ移動し、通常時の透明度を下げ、ホバー時に見やすく表示するようにした。

### 🧠 開発経緯・思考プロセス
- Eagle公式APIでは評価値が`star`として定義され、旧Web APIのitem/updateは評価更新を明示的にサポートしているため、今回の反映不具合には更新APIを採用した。
- 公式APIのローカルポートはEagleの起動状態が前提であるため、Eagle実機で保存後の再取得結果を確認する。

## [2026-07-25] - ビューアー操作と複数選択一括処理

### 🚀 追加・実装
- 拡大ビューアーにEagle本体で開く、ズーム、回転、スライドショーを追加した。
- 通常クリックとCtrl（macOSはCommand）クリックによる複数選択を追加した。
- 選択画像だけの連番リネーム、タグの一括追加・削除を追加した。
- Deleteキーまたはボタンで選択画像をEagleのゴミ箱へ移せるようにした。
- 選択中の複数画像へ評価を一括設定できるようにした。

### 🔧 変更・修正
- 単一選択用の`selectedId`に加えて`selectedIds`を持ち、既存のプレビュー操作と一括処理を両立した。
- ゴミ箱移動はEagle公式の`item.moveToTrash()`を使用し、完全削除を避けた。

### 🧠 開発経緯・思考プロセス
- Ctrl＋クリックは追加・解除、通常クリックは選択をリセットする一般的な操作感にした。
- プレビューのスライドショーは2.5秒間隔、ズームは25〜400%、回転は90度単位とした。
- TODO: Eagle実機で複数選択、タグ、キーボードDelete、スライドショーの挙動を確認する。

## [2026-07-25] - ビューアー移動・評価フィルター・未評価整理

### 🚀 追加・実装
- 拡大プレビュー画面に前後移動ボタンを追加し、左右キーでも画像を切り替えられるようにした。
- 評価フィルター（すべて、評価なし、★1〜★5）を追加した。
- 評価なしの画像をまとめてEagleのゴミ箱へ移す機能を追加した。

### 🔧 変更・修正
- フィルター中にドラッグしても、非表示画像を含む全体の保存順を壊さないよう、全件配列と表示配列を分離した。
- 削除は完全消去ではなく、Eagle公式の`item.moveToTrash()`を使う方式にした。

### 🧠 開発経緯・思考プロセス
- 通常ビューアーで最も利用頻度が高い前後移動を先に実装した。フィルター中は表示中の画像だけを移動対象にする。
- 今後の候補：プレビュー中の評価変更、Eagle本体で開く、回転・拡大率操作、選択画像だけの一括処理。

## [2026-07-25] - Eagle評価順並べ替えプロトタイプ

### 🚀 追加・実装
- `manifest.json`、`index.html`、`index.js`を追加。
- 選択中フォルダの画像を取得し、評価順で初期表示する機能を追加。
- HTML5ドラッグ＆ドロップによる手動並べ替えを追加。
- 「評価順に戻す」ボタンと、プラグイン内順序のlocalStorage保存を追加。
- 任意で順位をEagleの注釈へ保存する機能を追加。

### 🔧 変更・修正
- Eagle本体のカスタム順を直接変更する公開APIがないため、まずはプラグイン画面内の順序を管理する構成にした。

### 🧠 開発経緯・思考プロセス
- 外部ライブラリなしで試せるよう、Sortable.jsではなく標準HTML5ドラッグ＆ドロップを採用した。
- 注釈への順位保存は既存注釈への影響があるため、明示ボタンでのみ実行する。
- ドラッグ中の再描画を避け、dropイベント時にだけ順番を確定するよう調整した。
- 未配置のロゴ画像参照を外し、追加アセットなしで試せる構成にした。
- Eagle標準相当のソート選択、ドラッグ位置の挿入ライン表示、現在順での連番リネームを追加した。
- 注釈保存ボタンを削除し、Eagle本体の名前順ソートで順番を再現する方針に変更した。
- リネーム欄を単純な「後半の名前」入力へ変更し、`name`入力時に001_name、002_name形式で保存するようにした。
- ドラッグ位置マーカーをカード下端の線から、画像同士のすき間に表示する縦線へ変更した。
- 画像クリックによる選択状態、スペースキーの拡大プレビュー、Esc・背景クリックによる終了を追加した。
- スペースキーをトグル操作に変更し、拡大中のスペースで復帰できるようにした。
- 画像のダブルクリックで拡大、プレビューのダブルクリックで復帰できるようにした。
- 正式版向けにプラグインアイコンを追加し、バージョンを1.0.0へ更新した。
- `devTools`を無効化し、通常起動時に開発者用画面を表示しない設定にした。
- 評価専用に見えないよう、プラグイン名を「Flexible Sort Board」へ変更した。
- 選択中画像の評価を0〜5で変更・保存できるボタンと、数字キー・テンキー入力を追加した。
- 再読み込みボタンを`↻`アイコンへ簡略化した。
- TODO: Eagle上での実機確認、複数フォルダ選択時の扱い、既存の数字始まりファイル名に対するリネーム仕様の改善。
## [2026-07-28] - 一覧画像サイズと削除確認の改善

### 追加・実装 (Added/Implemented)
- 一覧表示の画像カード幅をスライダーで変更できる設定を追加し、フォルダごとにサイズを保存するようにした。

### 変更・修正 (Changed/Fixed)
- 一覧画像をトリミングせず、正方形の表示枠内に画像全体を表示するように変更した。
- 画像を1枚だけゴミ箱へ移動する場合は確認を省略し、複数枚の場合のみ確認ダイアログを表示するようにした。

## [2026-07-28] - 操作バー固定と一覧画像サイズ上限の拡張

### 追加・実装 (Added/Implemented)
- 一覧をスクロールしても、評価・並び替え・画像サイズ・削除などの操作バーが画面上部に固定されるようにした。

### 変更・修正 (Changed/Fixed)
- 一覧画像サイズスライダーの上限を360pxから800pxへ引き上げた。

## [2026-07-28] - 公開用コピーのスライダー上限不一致を修正

### 変更・修正 (Changed/Fixed)
- スライダーのHTML上限だけでなく、JavaScript側の上限値も800pxに統一した。
- Eagleで読み込まれる `publish/Flexible Sort Board` に最新のHTML・JavaScriptを反映した。

## [2026-07-31] - 複数選択画像の一括並べ替え

### 追加・実装 (Added/Implemented)
- Ctrl／Commandクリックで選択した複数画像を、表示順を保ったままドラッグ＆ドロップでまとめて移動できるようにした。

### 変更・修正 (Changed/Fixed)
- ドラッグ対象を1件のIDではなく選択中の表示画像群として扱い、移動先に一括挿入するよう変更した。
- 公開用コピー `publish/Flexible Sort Board` と操作説明を同期した。
- バージョンを1.1.1へ更新し、新しい `.eagleplugin` パッケージを作成した。

### 開発経緯・思考プロセス (Dev Notes & Background)
- 評価フィルター中は、表示中の選択画像だけを一括移動し、非表示画像の相対位置は従来どおり維持する。
